const FALLBACK_ITEM_CATALOG={whatsapp:{title:'Script inteligente de WhatsApp',unit_price:19.90,item_type:'card'}};
const send=(res,s,b)=>res.status(s).json(b);const norm=v=>({'script-whatsapp':'whatsapp','whatsapp-script':'whatsapp'}[String(v||'').trim().toLowerCase()]||String(v||'').trim().toLowerCase());
async function sbFetch(url,key,path,opt={}){return fetch(`${url}/rest/v1/${path}`,{...opt,headers:{apikey:key,'Content-Type':'application/json',...(opt.headers||{})}})}
async function getCatalogItem(su,sk,itemId){const r=await sbFetch(su,sk,`ecosystem_cards?item_id=eq.${encodeURIComponent(itemId)}&active=eq.true&select=item_id,title,price_cents,price_label,access_level`);if(r.ok){const rows=await r.json();const x=rows?.[0];if(x&&Number.isInteger(x.price_cents)&&x.price_cents>=0)return {title:x.title,unit_price:x.price_cents/100,item_type:'card'};}return FALLBACK_ITEM_CATALOG[itemId]||null;}
async function getMercadoPagoPayment(paymentId,accessToken){const r=await fetch(`https://api.mercadopago.com/v1/payments/${encodeURIComponent(paymentId)}`,{headers:{Authorization:`Bearer ${accessToken}`}});const data=await r.json().catch(()=>({}));return {ok:r.ok,status:r.status,data};}
async function syncApprovedPurchase({mpToken,su,sk,user,paymentId}){const pr=await getMercadoPagoPayment(paymentId,mpToken);if(!pr.ok)return {synced:false,reason:'payment_not_found_or_unavailable',mercado_pago_status:pr.status};const payment=pr.data||{};if(String(payment.status||'').toLowerCase()!=='approved')return {synced:false,reason:'payment_not_approved',payment_status:payment.status||null};const metadata=payment.metadata||{};let userId=String(metadata.miv_user_id||'').trim(),itemType=String(metadata.miv_item_type||'').trim().toLowerCase(),itemId=norm(metadata.miv_item_id);
 if((!userId||!itemType||!itemId)&&payment.external_reference){const parts=String(payment.external_reference).split('|');if(parts.length===3){userId=userId||String(parts[0]||'').trim();itemType=itemType||String(parts[1]||'').trim().toLowerCase();itemId=itemId||norm(parts[2]);}}
 if(userId!==user.id)return {statusCode:403,error:'Este pagamento não pertence à conta atual.'};
 const item=await getCatalogItem(su,sk,itemId);if(!item||itemType!==item.item_type){console.error('[MIV sync purchase] Metadados de compra inválidos.',{userId,itemId,itemType});return {synced:false,reason:'invalid_purchase_metadata'};}
 const paidCents=Math.round(Number(payment.transaction_amount||0)*100),expectedCents=Number(metadata.miv_expected_amount_cents??Math.round(item.unit_price*100));if(paidCents!==expectedCents){console.error('[MIV sync purchase] Valor divergente.',{itemId,paidCents,expected:expectedCents});return {synced:false,reason:'amount_mismatch'};}
 const row={user_id:userId,item_id:itemId,item_type:item.item_type,status:'paid',amount_cents:paidCents,provider:'mercadopago',provider_payment_id:String(payment.id||paymentId),purchased_at:payment.date_approved||new Date().toISOString()};const sr=await sbFetch(su,sk,'user_purchases?on_conflict=user_id,item_type,item_id',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify(row)});const sd=await sr.json().catch(()=>null);if(!sr.ok){console.error('[MIV sync purchase] Falha ao gravar compra no Supabase:',sr.status,sd);return {statusCode:500,error:'Falha ao registrar a compra.'};}
 const couponId=String(metadata.miv_coupon_id||'').trim();if(couponId){const er=await sbFetch(su,sk,`coupon_redemptions?coupon_id=eq.${encodeURIComponent(couponId)}&user_id=eq.${encodeURIComponent(userId)}&item_id=eq.${encodeURIComponent(itemId)}&select=id&limit=1`);const exists=er.ok?await er.json().catch(()=>[]):[];if(!exists?.length){const rr=await sbFetch(su,sk,'coupon_redemptions',{method:'POST',body:JSON.stringify({coupon_id:couponId,user_id:userId,item_id:itemId,purchase_id:sd?.[0]?.id||null,discount_cents:Number(metadata.miv_discount_cents||0),redeemed_at:new Date().toISOString()})});if(!rr.ok)console.error('[MIV sync purchase] Compra liberada, mas falhou registro do cupom:',await rr.text());}}
 console.log('[MIV sync purchase] Compra liberada:',{userId,itemId,providerPaymentId:row.provider_payment_id});return {synced:true,item_id:itemId};}

module.exports=async function(req,res){if(req.method!=='POST')return send(res,405,{error:'Método não permitido.'});try{
 const mpToken=process.env.MERCADOPAGO_ACCESS_TOKEN,su=process.env.SUPABASE_URL,pk=process.env.SUPABASE_PUBLISHABLE_KEY,sk=process.env.SUPABASE_SECRET_KEY;if(!mpToken||!su||!pk||!sk)return send(res,500,{error:'Configuração incompleta.'});
 const ah=req.headers.authorization||'',at=ah.startsWith('Bearer ')?ah.slice(7):'';if(!at)return send(res,401,{error:'Entre na sua conta para comprar.'});const ur=await fetch(`${su}/auth/v1/user`,{headers:{apikey:pk,Authorization:`Bearer ${at}`}});if(!ur.ok)return send(res,401,{error:'Sessão inválida ou expirada.'});const user=await ur.json();
 if(req.body?.action==='sync_purchase'){const paymentId=String(req.body?.payment_id||req.body?.collection_id||'').trim();if(!paymentId)return send(res,400,{error:'Pagamento não informado.'});const result=await syncApprovedPurchase({mpToken,su,sk,user,paymentId});return send(res,result.statusCode||200,result.error?{error:result.error}:result);}
 const itemId=norm(req.body?.item_id),item=await getCatalogItem(su,sk,itemId);if(!item)return send(res,400,{error:'Item não habilitado para pagamento ou sem preço fixo.'});let finalCents=Math.round(item.unit_price*100),coupon=null,discountCents=0;const code=String(req.body?.coupon_code||'').trim().toUpperCase();
 if(code){const cr=await sbFetch(su,sk,`coupons?code=eq.${encodeURIComponent(code)}&active=eq.true&select=*`);const rows=await cr.json();if(!cr.ok){console.error('Supabase coupons query failed',cr.status,rows);return send(res,502,{error:'Falha ao consultar cupons no banco.',code:'coupon_db_error',supabase_status:cr.status});}coupon=rows?.[0];if(!coupon)return send(res,400,{error:'Cupom inválido ou inativo.',code:'coupon_not_found'});const now=Date.now();if(coupon.starts_at&&new Date(coupon.starts_at).getTime()>now)return send(res,400,{error:'Este cupom ainda não está válido.'});if(coupon.expires_at&&new Date(coupon.expires_at).getTime()<now)return send(res,400,{error:'Este cupom expirou.'});if(coupon.item_id&&norm(coupon.item_id)!==itemId)return send(res,400,{error:'Este cupom não vale para este item.'});
  const rr=await sbFetch(su,sk,`coupon_redemptions?coupon_id=eq.${coupon.id}&select=user_id`);const reds=await rr.json();if(coupon.max_uses&&reds.length>=coupon.max_uses)return send(res,400,{error:'Este cupom atingiu o limite de usos.'});if(reds.filter(x=>x.user_id===user.id).length>=coupon.max_uses_per_user)return send(res,400,{error:'Você já utilizou este cupom.'});discountCents=coupon.discount_type==='percent'?Math.round(finalCents*Math.min(Number(coupon.discount_value),100)/100):Math.round(Number(coupon.discount_value)*100);discountCents=Math.min(discountCents,finalCents);finalCents-=discountCents;
 }
 const meta={miv_user_id:user.id,miv_item_id:itemId,miv_item_type:item.item_type,miv_expected_amount_cents:finalCents,miv_coupon_id:coupon?.id||'',miv_coupon_code:coupon?.code||'',miv_discount_cents:discountCents};
 if(finalCents===0){const row={user_id:user.id,item_id:itemId,item_type:item.item_type,status:'paid',amount_cents:0,provider:'coupon_100',provider_payment_id:`coupon:${coupon.id}:${user.id}:${Date.now()}`,purchased_at:new Date().toISOString()};const pr=await sbFetch(su,sk,'user_purchases?on_conflict=user_id,item_type,item_id',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify(row)});const pd=await pr.json();if(!pr.ok)return send(res,500,{error:'Não foi possível liberar o item.'});await sbFetch(su,sk,'coupon_redemptions',{method:'POST',body:JSON.stringify({coupon_id:coupon.id,user_id:user.id,item_id:itemId,purchase_id:pd?.[0]?.id||null,discount_cents:discountCents})});return send(res,200,{free_unlock:true,item_id:itemId,amount:0});}
 const host=String(req.headers['x-forwarded-host']||req.headers.host||'miv-ecosystem.vercel.app').trim();
 const proto=String(req.headers['x-forwarded-proto']||'https').split(',')[0].trim()||'https';
 const site=`${proto}://${host}`;
 const testPayerEmail=String(process.env.MERCADOPAGO_TEST_PAYER_EMAIL||'').trim();
 const isTestToken=/^TEST-/i.test(String(mpToken).trim());
 const payerEmail=(isTestToken&&testPayerEmail)?testPayerEmail:user.email;
 const pref={items:[{id:itemId,title:item.title,quantity:1,currency_id:'BRL',unit_price:finalCents/100}],payer:payerEmail?{email:payerEmail}:undefined,external_reference:`${user.id}|${item.item_type}|${itemId}`,metadata:meta,back_urls:{success:`${site}/?payment=success#central`,pending:`${site}/?payment=pending#central`,failure:`${site}/?payment=failure#central`},auto_return:'approved',notification_url:`${site}/api/mercadopago/webhook`,statement_descriptor:'MIV ECOSYSTEM'};const mr=await fetch('https://api.mercadopago.com/checkout/preferences',{method:'POST',headers:{Authorization:`Bearer ${mpToken}`,'Content-Type':'application/json'},body:JSON.stringify(pref)});const md=await mr.json();if(!mr.ok)return send(res,502,{error:'Mercado Pago não criou o checkout.',details:md?.message||null});return send(res,200,{preference_id:md.id,checkout_url:md.sandbox_init_point||md.init_point,item_id:itemId,amount:finalCents/100,discount:discountCents/100,coupon:coupon?.code||null});
}catch(e){console.error(e);return send(res,500,{error:'Não foi possível iniciar o pagamento.'})}};
